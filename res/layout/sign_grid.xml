<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >
    <LinearLayout 
        android:id="@+id/sign_grid_linear"
        android:orientation="horizontal"
        android:layout_width="fill_parent"
        android:layout_height="70dip"
        android:layout_alignParentTop="true"
        android:background="@drawable/edit_yes"
        android:layout_marginBottom="20dip"
        >
"
        <ImageView android:id="@+id/sign_grid_imag" 
            android:layout_width="50dip"
            android:layout_height="50dip"
            android:layout_gravity="top"
            android:background="@drawable/ic_launcher"
            />
        <TextView android:id="@+id/sign_grid_text"
            android:layout_width="match_parent"
            android:layout_height="fill_parent"
            android:gravity="center_vertical"
            android:layout_marginLeft="10dip"
            android:textSize="18sp"
            android:textColor="#6699ff"
            android:text="@string/icon"
            />
    </LinearLayout>
	<GridView android:id="@+id/sign_grid_gridview"
	    android:layout_width="wrap_content"
	    android:layout_height="fill_parent"
	    android:numColumns="4"
	    android:gravity="center"
	    android:layout_below="@id/sign_grid_linear"   
	    />
</RelativeLayout>
